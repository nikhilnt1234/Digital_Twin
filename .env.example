# Coach Chat (Gemini proxy) - server-only, never exposed to client
GEMINI_API_KEY=

# Clinical Analysis (MedGemma provider)
# When true, uses rule-based demo analysis (no network). Default: true.
MEDGEMMA_DEMO_MODE=true

# Remote MedGemma endpoint when MEDGEMMA_DEMO_MODE=false
# Example: https://your-medgemma-service.example.com
MEDGEMMA_ENDPOINT=

# Timeout for MedGemma API calls (ms). Default: 20000
MEDGEMMA_TIMEOUT_MS=20000

# Legacy: MedGemma local Python service URL (used by /api/analyze-checkin)
# MEDGEMMA_URL=http://127.0.0.1:8000
